import {StyleSheet} from 'react-native';
import {leftSwipeAction} from './leftSwipeAction';

export default StyleSheet.create({
  rightSwipeContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'blue',
    paddingRight: 20,
  },
  rightSwipeButton: {
    padding: 10,
  },
});
